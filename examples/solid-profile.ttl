# This is an example of a user's main profile document (their WebID).
# This file would typically be located at the root of their Pod, e.g., /profile/card

@prefix foaf: <http://xmlns.com/foaf/0.1/> .
@prefix solid: <http://www.w3.org/ns/solid/terms#> .
@prefix pim: <http://www.w3.org/ns/pim/space#> .
@prefix : <#> .

# The main subject of this document is the person themselves, identified by the fragment '#me'.
:me
    a foaf:Person;
    foaf:name "Alice" ;

    # This triple points to the user's main storage space (their Pod root).
    pim:storage </> ;

    # This is the crucial link to the Public Type Index.
    # It tells applications where to look to discover data locations.
    solid:publicTypeIndex </settings/publicTypeIndex> .
